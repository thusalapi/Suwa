# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
.next/
out/
dist/
build/

# Environment
.env
.env*.local
.env.production

# Misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel
*.tsbuildinfo
next-env.d.ts

# Drizzle local
drizzle/.cache

# Local database backups (encrypted dumps + restore temp) — never commit clinical data
backups/

# Liquibase local config (holds the DB password)
liquibase/liquibase.properties

# design-sync (Claude Design) — staged scripts, build output, machine state
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
